# AgeBand — agent service dependencies
# Pin versions for reproducibility; update on explicit decision only.

# Core agent framework
tinyagent>=0.1.0
openai>=1.40.0
openai-agents>=0.0.15

# Data modelling
pydantic>=2.7.0

# Web serving
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0

# Quality gates (also used in CI)
mypy>=1.10.0
ruff>=0.4.9
black>=24.4.2
radon>=6.0.1
pytest>=8.2.2
pytest-cov>=5.0.0
pytest-asyncio>=0.23.7

# Utilities
python-dotenv>=1.0.1

# Language detection (optional — graceful fallback to Unicode-block heuristics
# when unavailable). Required for es/fr/de and other Latin-script language
# detection that Unicode blocks cannot distinguish from English.
langdetect>=1.0.9
