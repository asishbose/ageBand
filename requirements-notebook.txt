# AgeBand demo notebook — additional dependencies.
# These are NOT added to requirements.txt because the notebook is an optional
# local dev/demo tool, not a production service dependency.
#
# Install alongside the main requirements:
#   pip install -r requirements.txt -r requirements-notebook.txt

jupyterlab>=4.2.0
ipywidgets>=8.1.0
pandas>=2.2.0

# HTTP client for the scenario walkthrough cells (stdlib urllib works too,
# but requests gives cleaner notebook-cell code).
requests>=2.32.0
