apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ageband.serviceAccountName" . }}
  labels:
    {{- include "ageband.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
